pipeline{
    agent any
    stages{
        stage("code"){
            steps{
                echo "This is code stage"
            }
        }
    }
    post {
    success {
        mail(
            to: "sudharsansubramanian2000@gmail.com",
            subject: "✅ SUCCESS: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
            body: """
Good news!

Job: ${env.JOB_NAME}
Build: #${env.BUILD_NUMBER}
Status: SUCCESS

View build:
${env.BUILD_URL}
"""
        )
    }

    failure {
        mail(
            to: "sudharsansubramanian2000@gmail.com",
            subject: "❌ FAILED: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
            body: """
Oops! Build failed.

Job: ${env.JOB_NAME}
Build: #${env.BUILD_NUMBER}
Status: FAILED

Check logs:
${env.BUILD_URL}console
"""
        )
    }
}
}
